kind: Service
apiVersion: v1
metadata:
  name: acme-controller
  namespace: acme
spec:
  selector:
    app: acme-controller
  ports:
  - name: http
    protocol: TCP 
    port: 80
    targetPort: 5000

